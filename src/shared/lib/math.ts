export function addCents(a: number, b: number): number {
  return a + b;
}

export function subtractCents(a: number, b: number): number {
  return a - b;
}

export function multiplySeats(cents: number, seats: number): number {
  return cents * seats;
}
